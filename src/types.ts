import {
    TypeString,
    TypeNumber,
    TypeArray,
    TypeGeneral,
} from './util'

const String = TypeString
const Number = TypeNumber
const Array = TypeArray
const General = TypeGeneral

export const Types = {
    String,
    Number,
    Array,
    General
}